//métodos alert, confirm e prompt - (console do navegador);

/**
 * exercício:
 * 
 * TODO:
 * 1 - Pedir para o usuário digitar 2 números;
 * 2 - Somar os 2 números
 * 3 - Exibir o resultado
 */

const n1 = prompt('Digite o primeiro número:');
const n2 = prompt('Digite o segundo número:');
const resultado = parseInt(n1) + parseInt(n2);

alert(`Resultado: ${resultado}`);
