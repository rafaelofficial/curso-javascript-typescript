// dados primitivos - string, number, undefined, null, boolean

const nome = 'Rafael'; // string
const num1 = 10; // number
const num2 = 10.0; // number
let nomeAluno; // undefined -> não aponta pra nunhum local da memória
const sobrenomeAluno = null; // nulo -> também não aponta pra nunhum local da memória
const aprovado = false; // boleano -> true, false (lógico)
